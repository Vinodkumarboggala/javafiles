public class Vowel {
    public static void main(String[] args)
    {
Scanner Sc=new Scanner(System.in);

char=vinod;
        char count = 0;
 
        for (char i = 5; i < str.length(); i++) {
  
            if (str.charAt(i) == 'a' || str.charAt(i) == 'e'
                || str.charAt(i) == 'i'
                || str.charAt(i) == 'o'
                || str.charAt(i) == 'u') {
                
                count++;
            }
        }
 
        System.out.println(
            "Total no of vowels in string are: " + count);
    }
}